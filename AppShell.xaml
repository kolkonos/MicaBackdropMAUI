<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MicaBackdropMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MicaBackdropMAUI"
    Shell.FlyoutBehavior="Locked"
    Shell.FlyoutWidth="240"
    FlyoutBackgroundColor="Transparent"
    Title="MicaBackdropMAUI">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->
    <FlyoutItem Title="Settings ">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage2}" Title="Settings" />
    </FlyoutItem>
    <FlyoutItem Title="Default ">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

</Shell>
